% ============================================================
% BlackboxNLP 2026 submission — working draft
% Rewritten july3 (streamlined arc); previous version: main-old.tex.bak
%
% Build: tectonic main.tex (or pdflatex/latexmk)
% NOTE (jul10): now on the official ACL style (acl.sty + acl_natbib.bst vendored
% in this dir; [review] option = line numbers). Pre-ACL snapshots:
% main-pre-acl.tex.bak / appendix_experiments-pre-acl.tex.bak.
%
% Convention: the abstract is prose (Hadi's); everything else is \outline{}
% bullets that Hadi converts to prose. Claude maintains structure/refs/numbers.
% Page budgets in section headings (X.Xp) assume ACL LONG format: 8 pages of
% content + unlimited Limitations/references. Current allocation ~8.5p counted
% -> trim ~0.5p in the prose pass (Background and Pilot are the flex).
% Bullet tags: (half para) / (1 para) / (2 paras) = target length in prose.
%
% Narrative arc: phenomenon (unexplained steering variance) -> inventory
% (~600 concepts, 24 supersenses, dual contrasts) -> two-config steering ->
% regression (which concepts steer) -> pilot interpretation (parent-dominance;
% coherence != identity) -> causal probe (within-category rescue).
% ============================================================
\documentclass[11pt]{article}
\usepackage[review]{acl}   % BlackboxNLP uses the ACL template; [review] = line numbers + anonymity off for drafting? (review shows line numbers; use [final] for camera-ready)
\usepackage{times}
\usepackage{latexsym}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{amssymb}
\usepackage[normalem]{ulem}  % \sout{...} available; convention: strike, don't delete
\usepackage{graphicx}
\graphicspath{{../plots/}}  % figures live in repo-root plots/

% outline bullets render gray so they're visually distinct from prose
\newenvironment{outline}{\color{gray!70!black}\begin{itemize}[leftmargin=1.2em,itemsep=1pt]}{\end{itemize}\color{black}}

\title{Which Imagined Concepts Can Be Steered in LLMs?}
\author{Hadi Asghari \\ Alexander von Humboldt Institute for Internet and Society \\ \texttt{...} \And ... \\}

\begin{document}
\maketitle

% Figure 1 (option 1 of 3; opts 2-3 in plots/). Regenerated jul10 with v4-run data
% (plots/make_fig1.py; annotations asserted against the judged outcomes).
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig1_opt1.png}
\caption{Concepts steered per WordNet supersense (of 25 each; steered = judged
as expressing the concept under either evaluation prompt, V1$\cup$V6; all-layer
steering, v4 extraction frame): steerability varies systematically by category,
and the rates and much of the ordering replicate across the two model families.}
\label{fig:fig1}
\end{figure*}

\begin{abstract} % (0.5p incl. title block)
Steering vectors are widely used to control and interpret LLMs, yet benchmarks show that some concepts steer reliably while others barely respond --- a variance that is reported but rarely explained. In this research we investigate which kinds of concepts are steerable in Llama-3.1-8B and Gemma-2-9B. We use the RFM method to extract steering vectors (or directions) for approximately 600 concepts drawn from 24 WordNet supersense categories, and validate which of them successfully steer the model. We then build a regression model explaining steerability from concept category, concreteness, polysemy, and word frequency, as well as the share of the top eigenvector during extraction ($\lambda_1$/trace). The results help distinguish concepts that behave as single directions --- ``primitive'' for the model --- from those that are composite or context-dependent, and offer practitioners an extraction-time indication of when single-vector steering can be trusted.

\begin{outline}
\item \textbf{TODO: the prose paragraph above predates the final results --- update: add age-of-acquisition to the predictor list, replace ``$\lambda_1$/trace'' with ``spectrum concentration (participation ratio)'', and consider adding one sentence each on the depth-window finding and the cross-model replication.}
\item \textbf{Headline: 41.5\% of the 600 concepts steer on Llama (249/600, V1$\cup$V6) and 37.3\% on Gemma (224/600) under the v4 instrument, and the psycholinguistic laws replicate across BOTH models with all four predictors significant: frequency $+$, concreteness $-$, age-of-acquisition $-$, polysemy $-$. Spectrum concentration (PR) predicts steerability within both models, at model-specific depths. Frequent, early-acquired, monosemous, abstract concepts steer best.}
\item Candidate sentence (failure mode): when steering fails, it fails informatively --- delivery of the category/frame blend, a diffuse discriminative spectrum, or injection outside the concept's depth window; not silence. (1 sentence)
\item Candidate sentence (instrument): measured steerability is instrument-relative --- one changed frame sentence doubles the rate on both models --- but the laws survive instrument and model change. (1 sentence)
\item Candidate sentence (depth): concepts are injectable in depth windows ordered by abstractness; one well-chosen layer nearly matches all-layer steering. (1 sentence)
\end{outline}
\end{abstract}

\section{Introduction (1.0p)}
\begin{outline}
\item Hook: steering/RePE papers throw heterogeneous concept lists at models; some concepts steer at 90\%, others at chance. The variance is treated as noise; we argue it is signal. (1 para)
\item The implicit assumption of top-1 eigenvector / PCA-1 / difference-of-means steering: the concept is representable as \emph{one direction}, injectable with \emph{one coefficient}, at whatever layer(s) the method uses. (half para)
\item Existing evidence that not all features are one-dimensional (Engels et al.) --- but no one has asked \emph{which} concepts behave as single directions, or whether this aligns with human semantic structure. (half para)
\item Our question: is steerability systematic and predictable from the linguistic properties of the word --- and what does a steering failure actually consist of? (half para)
\item Contributions (short lead-in + three numbered items, 2--3 sentences each): (1 para)
  \begin{itemize}
  \item C1: a systematic steerability study --- $\sim$600 lexicalized concepts across 24 WordNet supersenses, annotated with psycholinguistic norms, steered under a controlled protocol on two models --- together with a 7-frame instrument study (Table~\ref{tab:framegrid}) showing that the extraction contrast itself sets measured steerability ($2\times$ rate swing at fixed method and model); per-concept results and code released.
  \item C2: a regression explaining steerability from frequency, concreteness, age-of-acquisition, and polysemy (all significant, signs replicating across Llama and Gemma), with extraction-internal spectral concentration (participation ratio / $\lambda_1$-share) as a zero-cost predictor that beats the lexical block.
  \item C3: a mechanistic account of what failure looks like --- parent-dominance of extracted directions, coherence $\neq$ identity, injection outside the concept's depth window --- probed causally (within-category subtraction; single-layer depth sweep).
  \end{itemize}
\end{outline}

\section{Background and Related Work (1.0p)}
% Convention per bullet: cited line(s) + one closing sentence on same-vs-different.
\begin{outline}
\item \textbf{Linear representation hypothesis and steering.} \citet{park2024linear}; RepE \citep{zou2023repe}; CAA \citep{panickssery2024caa}; ActAdd \citep{turner2023actadd}; ITI \citep{li2023iti}; the RFM benchmark we build on \citep{beaglehole2025universal}: kernel ridge + AGOP, top eigenvector as steering vector. \emph{Ours:} we adopt RFM unchanged and ask \emph{which concepts} it works for. Second relation: steering rates are \emph{instrument-relative} --- one changed contrast sentence doubles the rate (19.8\%$\to$41.5\%) at fixed method, model, and judge; method comparisons with hand-tuned per-class prompts inherit this confound. (1 para)
\item \textbf{Multi-dimensional features.} \citet{engels2024notalllinear}: not all features are one-dimensional. \emph{Ours:} we test the premise on a stratified inventory and tie it to word properties: spectrum concentration (PR / $\lambda_1$-share) predicts steering in both models --- ``effectively one-dimensional'' is measurable and graded, and polysemy acts on steerability largely \emph{through} it. (half para)
\item \textbf{Hierarchical concept geometry.} \citet{park2025geometry}: $\sim$900 WordNet concepts, Llama-3 and Gemma; hierarchically related concepts occupy orthogonal subspaces --- the representational-geometry twin of this study. \emph{Ours:} they characterize how hierarchy is \emph{encoded}; we test what it predicts about \emph{intervention}. Our parent-dominance finding is the steering-side counterpart of their parent/child decomposition; and our R4 probe reads their orthogonality causally --- the child-specific residual exists but is rarely independently injectable (9 gained / 14 lost of 150). (half para)
\item \textbf{Predicting steerability.} Directional agreement and geometric predictors of steering (un)reliability \citep{braun2025unreliability,braun2026geometric} --- the closest neighbors to our R3; AxBench \citep{wu2025axbench} for large-scale evaluation. \emph{Ours:} we predict from the concept's \emph{linguistic structure}; the lexical laws (frequency $+$, concreteness $-$, AoA $-$, polysemy $-$) replicate across two models and two instruments --- portable predictors engineering signals cannot supply. We add a caution: geometric predictors are only interpretable relative to the contrast design ($\lambda_1$-share flipped null$\to$significant with the frame). AxBench positioning: they sample concepts \emph{from the model} (SAE-latent labels; no norms, no stratification) and vary methods; we sample from the \emph{lexicon} and fix the method --- only the latter can ask which kinds of concepts steer. Their trained ReFT-r1 bounds our claims: ``unsteerable'' means extraction-based rank-1 steering fails. \textbf{TODO cite:} LAP / logit-lens accessibility (Billa 2026); ``When is your LLM steerable?'' (bib stubs). (1 para)
\item \textbf{Causal identification of concept vectors.} Function vectors \citep{todd2024function}; relation decoding \citep{hernandez2024linearity}; ROME \citep{meng2022rome}. \emph{Ours:} we identify by contrast design, probe by intervention (R4). The depth sweep converges with causal tracing: entity content is injectable in a sharp mid-stack window (depths 14--17 of 32), where tracing localizes subject enrichment --- but read-side statistics cannot substitute for write-side tests. (half para)
\item \textbf{Frequency and linear representations.} Co-occurrence thresholds; long-tail knowledge \citep{kandpal2023longtail}. \emph{Ours:} frequency is a robust \emph{positive} predictor in both models --- against our pilot expectation, with co-occurrence-threshold accounts --- and thins when spectrum concentration enters: exposure appears to work partly \emph{through} representational consolidation. (half para)
\item \textbf{LLM--human alignment and psycholinguistic norms.} \citet{shani2025tokens} compare LLM and human concepts on input-side embeddings. \emph{Ours:} mid-stream and causal --- and human structure does predict what can be steered: early-acquired, frequent words steer best (AoA $\beta{=}-0.33/-0.49$ across models), over and above frequency. (half para)
\item \textbf{Methodological caution on cosine.} \citet{you2025semantics}: cosine misleads in anisotropic spaces. \emph{Ours:} geometry claims rest on \emph{differential} cosines under identical measurement, with the between-class value (0.35) as the anisotropy control. (half para)
\item Gap statement: no prior work connects concept-level semantic structure to steering outcome or explains what failure consists of. Our answer: (i) frequency, AoA, polysemy, and concreteness jointly predict steerability, replicating across Llama and Gemma; (ii) failure is usually \emph{informative} --- category/frame delivery, a diffuse spectrum, or injection outside the depth window --- not silence. (half para)
\end{outline}

\section{Defining and Sampling Concepts (1.0p)}
\begin{outline}
\item \textbf{Operational definition.} Sidestep the philosophical debate (Murphy 2002; Margolis \& Laurence 1999): a \emph{lexicalized concept} = a meaning a speech community assigned a word to, operationalized as a WordNet synset; each word mapped to its dominant (first) noun sense. (half para)
\item \textbf{Inventory.} All single-word, noun-dominant lemmas from WordNet; category = supersense (26 lexicographer files; Miller 1990; Fellbaum 1998; ``supersense'': Ciaramita \& Johnson 2003). Exclusions: \texttt{noun.Tops}; POS-ambiguous lemmas; small cells sampled exhaustively and flagged. (half para)
\item \textbf{Sample ($\approx$600, flat).} 25/supersense across $\sim$24 usable supersenses; within each supersense, stratify over frequency so category and frequency are not confounded. Six supersenses are pre-specified (before extraction) as the \emph{featured} cells for within-category analyses: \texttt{location}, \texttt{person}, \texttt{feeling} (pilot continuity) and \texttt{artifact}, \texttt{cognition}, \texttt{food} (concreteness extremes). At $n{=}25$ every cell is adequately powered for class means and absorbs attrition (prompted-ceiling/judge exclusions). (1 para)
\item \textbf{Predictors} (one table): Zipf frequency \citep{speer2018wordfreq}; concreteness \citep{brysbaert2014concreteness}; age of acquisition \citep{kuperman2012aoa}; polysemy (\#WordNet senses); supersense; log category size (\#noun synsets in the lexicographer file, covariate). Tested and null: token count, verb-sense count. (half para + table)
\item \textbf{Contrast design.} Positives wrap a statement in a universal frame; the frame must be felicitous across \emph{all} supersenses, else frame felicity confounds category; negatives are the bare statements (benchmark-comparable). Rationale for care here: a contrast in which frame, category, and instance always co-occur cannot statistically separate them (see Pilot) --- whatever the frame adds constantly ends up inside every direction. A second negative design --- same frame with a \emph{different} same-supersense concept, which cancels frame and category parent --- is used as the causal probe (R4), not the main contrast. (1 para)
\item \textbf{[NEW jul10] Main-run frame: v4 = ``Imagine/assume: \{c\}.''\ (constant prefix, concept unquoted; negatives bare).} Selected from a 7-frame grid on the pilot (Table~\ref{tab:framegrid}, Appendix~B): 77/90 V1$\cup$V6 vs.\ 61 (orig class-specific) and 59 (v2b dual frame) --- the only frame at ceiling on moods \emph{and} far ahead on places. Ablations attribute the gain to \emph{assume} (bare-noun reading $\approx$ role-assignment, a class-neutral persona prime) and show quoting the concept costs $\sim$7 points (mention-mode suppresses enactment). Trade-off, quantified in Appendix~B: a constant prefix re-saturates extraction $R^2$ (the prefix is a free class separator), so the dual-frame diagnostics (informative val-$r$) are given up in exchange for steering power; the frame-leak caveat transfers to the imagine/assume register (acknowledged in Limitations).
\item \textbf{Pilot/confirmatory split.} Pilot = 90 concepts (30 each: places, moods, personalities) from the released benchmark lists, used for all exploration; the WordNet sample is confirmatory, untouched during exploration. (half para)
\end{outline}



\section{Method (1.0p)}
\begin{outline}
\item \textbf{Direction extraction (RFM) --- and why RFM.} Per layer: kernel ridge regression on concept-vs-negative prompts; AGOP $M = \mathbb{E}[\nabla f \nabla f^{\top}]$; steering vector = top eigenvector of $M$. Chosen among RepE methods because (a) it is the method of the benchmark we build on (comparability); (b) unlike difference-of-means or PCA-1 it returns the \emph{full discriminative spectrum} with eigenvalues --- the spectral metrics below come for free, and the method is self-diagnosing (the pilot's confound was discovered through them); (c) its steering performance matches or beats the simpler extractors on the released benchmark. (1 para)
\item \textbf{Spectral metrics.} $\lambda_1/\mathrm{tr}(M)$, $\lambda_1/\lambda_2$, and the participation ratio $\mathrm{tr}(M)^2/\|M\|_F^2$; per-concept score read at a fixed relative depth (per-layer profile retained; the informative depth is model-specific --- 40\% on Llama, 60\% on Gemma). These measure the \emph{shape} of the discriminative spectrum, not concept identity (see Pilot); they are near-deterministic functions of one another ($|r| \geq .94$), so they enter analyses one at a time. (half para)
\item \textbf{Fixed hyperparameters --- a methodological requirement.} Kernel bandwidth inflates $\lambda_1$/trace mechanically (large bandwidth $\Rightarrow$ near-linear fit $\Rightarrow$ rank-1 AGOP); grid search over near-tied validation correlations injects artifact variance into the dependent measure. All extractions at bw=1, norm=True, reg=$10^{-3}$; vector and metrics read at a fixed (converged) iteration. (half para)
\item \textbf{Two-config steering protocol.} Each concept steered under (i) \emph{all layers}, benchmark defaults (unit vectors, released coefficient grid --- comparability); (ii) \emph{one mid layer} at $\approx$40\% depth (vector scaled by the concept's per-layer projection gap, cf.\ CAA / ITI; effective single-site dose $\approx$1$\times$ local residual-stream norm). $\Delta$(all $-$ single) measures how \emph{distributed} the concept's steerable content is --- with the caveat, added after the depth sweep, that $\Delta$ partly measures \emph{window placement}: the confirmatory single-site arm should sit at depth 15, inside the entity window. Two evaluation prompts (category-inviting V1; neutral V6; listed verbatim); greedy decoding. (1 para)
\item \textbf{Outcome.} Primary: steered under V1 or V6 at any coefficient (binary; the union de-biases the evaluation-relativity of either prompt alone, V1--V6 $\phi{=}0.59$). The binary criterion flattens near-misses (pilot: Edinburgh$\to$``Scottish Highlands'' scores 0); a graded rubric (V8) re-judges the same generations as a robustness check (Appendix~E). Verbatim prompts in Appendix~G. (1 para)
\item \textbf{Judge.} gpt-oss:20b (local, on the experiment GPU); symmetric judge question across supersenses. Validated against Claude Sonnet~5 on a 360-item set: 87--91\% agreement, $\kappa = .72$--$.78$; a stricter candidate (Haiku 4.5) was rejected for one-directional divergence from the two-judge consensus. Confirmatory-study additions: re-validation on $\sim$200 WordNet-distribution items + human spot-check of disagreements (Appendix~F). (half para)
\item \textbf{Controls.} Prompted ceiling (can the model express the concept when explicitly asked?) --- steerability conditional on ceiling; pipeline sanity-checked on known-good concepts per model. \textbf{TODO: the prompted-ceiling control is promised here but has not been run at $n{=}600$ --- either run it (one cheap generation pass, no steering) or cut the promise. It matters most for the bottom cells: is \texttt{noun.body} unsteerable or merely unpromptable?} (half para)
\item \textbf{Models.} Full protocol on Llama-3.1-8B-Instruct; headline analyses replicated on Gemma-2-9B-it. (half para)
\end{outline}

\section{Pilot Diagnostics and Protocol Selection (1.0p)}
% All pilot experiments: N = 30x3 (places/moods/personalities), one 90-concept sample.
% Behavioral arms re-run at N=90 pending; interim n=15 shakedown numbers are provisional.
\begin{outline}
\item \textbf{Hyper-parameter selection (summary).} Validation $r$ saturates ($\sim$0.97--0.99) across bandwidths, statement counts, and iterations $\Rightarrow$ selection by validation is third-decimal noise; large bandwidth forces rank-1 AGOP by construction; converged readout cuts metric instability 3--4$\times$. Decision rule: fixed modal setting, converged readout. Details in Appendix~A. (NB the saturation itself is frame-structural: any constant-prefix contrast hands the classifier a free separator; dual frames de-saturate val-$r$ --- Appendix~B.) (half para)
\item \textbf{Finding 1: parent-dominance.} Different same-category concepts' extracted top-1 directions are nearly identical (places: cosine $0.97 \to 0.84 \to 0.72$ across depth; \emph{all} top-3 components $\geq$0.90 shared). Single-layer dose-response saturates at the category \emph{prototype} (every city $\to$ ``quiet beach at sunset'') with region-level near-misses (Edinburgh$\to$Dublin pub); giving secondary components injection mass adds a generic roleplay axis, not instance content. Three legs: geometry, dose-response, component ablation. Interpretation: a contrast in which frame, parent, and instance always co-occur is statistically one axis --- what steering delivers is mostly the category. (1 para + figure \texttt{parent\_share\_by\_depth.png})
\item \textbf{Finding 2: coherence $\neq$ identity.} Places show the \emph{highest} band $\lambda_1$/trace yet fail single-layer steering; moods (lower) succeed. Spectral metrics certify that a \emph{coherent} (context-invariant, rank-1) direction was extracted --- not that it is the judged content. Consequence: their regression coefficients are expected to be conditional on contrast type; tested under both contrasts. (1 para)
\item \textbf{Finding 3: attribute vs.\ instance.} Attribute concepts (moods) steer from one mid layer; instance identities steer only under all-layer injection, reaching region granularity --- consistent with multi-stage assembly (per-layer residue accumulation + late lexical tipping + autoregressive bootstrap once the name is emitted). Motivates the two-config protocol and $\Delta$. (1 para)
\item \textbf{Protocol justifications} (one line each; evidence in Appendix~C): single mid layer in the mid-stack behavioral window --- the depth sweep (Discussion) later located it precisely at depths 14--17 for entity concepts, with the original 40\% (depth-13) site just below its onset; 60\% yields token-bias then repetition collapse. No top-$k$ arms --- the top-3 subspace under the released contrast is category-shared. Coefficient $\approx$1$\times$ local residual norm via per-layer magn scaling. Granularity as secondary outcome --- the binary judge flattens near-misses. (1 para)
\end{outline}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{parent_share_by_depth.png}
\caption{\textbf{Pilot Finding 1 (parent-dominance).} Cosine similarity of
same-category concepts' extracted top-1 directions across depth: the released
contrast yields largely \emph{shared} (category) directions for places, while
moods individuate.}
\label{fig:parentshare}
\end{figure}

\section{Results (1.5p)}



\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{covariate_boxplots_fv4.png}
\caption{\textbf{[NEW jul10] R2/R3: covariates by steering outcome} (v4 runs,
$n{=}600$ per model; PR at each model's informative depth). Zipf, AoA, and PR
separate univariately in both models; polysemy and concreteness do not (their
regression effects are conditional on correlated covariates).}
\label{fig:covariates}
\end{figure*}

\begin{outline}
\item \textbf{R1: Steerability varies systematically --- 249/600 (41.5\%) steer} (Llama, all-layer, v4 frame, judge = gpt-oss; V1 191, V6 195, union V1$\cup$V6; Figure~\ref{fig:fig1}). Per-supersense spread 2--21 of 25: top \texttt{noun.feeling} (21), \texttt{noun.person} (17), \texttt{noun.animal} (16); bottom \texttt{noun.body} (2), \texttt{noun.shape} (4), \texttt{noun.quantity}/\texttt{noun.relation} (5). (1 para + figure)
\item \textbf{R1 instrument comparison (v2b arm): the frame moves the rate, much less the ordering.} Under the dual v2b frame the same protocol yields 119/600 (19.8\%; spread 2--9); switching to v4 improves 22 of 24 supersenses while the ranking stays broadly stable (feeling top, body bottom under both; \texttt{noun.body} immovable at 2/25; Figure~\ref{fig:supersense}, Appendix~B). V1--V6 agreement $\phi = 0.59$ --- measured steerability is partly evaluation-relative, motivating the union outcome. (half para)
\item \textbf{R2: Psycholinguistic predictors (Table~\ref{tab:reg600v4}): all four lexical predictors significant, same signs in both models.} Llama: conc $-0.20^{*}$, zipf $+0.28^{*}$, AoA $-0.33^{**}$, polysemy $-0.30^{**}$; Gemma: conc $-0.20^{*}$, zipf $+0.23^{\dagger}$, AoA $-0.49^{**}$, polysemy $-0.35^{**}$. \textbf{The pilot expectation (concreteness $+$, polysemy carrying the signal, frequency null) is rejected: frequent-but-abstract concepts steer best.} AoA and polysemy were null under v2b and surface once the instrument improves (v2b: zipf $+0.40^{*}$, conc $-0.28^{*}$; Appendix~B). Honest-reporting nuance (Figure~\ref{fig:covariates}): polysemy and concreteness do \emph{not} separate univariately (MW $p{>}.6$; effects conditional on correlated covariates); zipf, AoA, PR separate cleanly in both models ($p{<}.005$). Collinearity among lexical predictors is benign (conc--zipf $r{=}.09$; VIF $<2.7$). Log category size is a positive predictor in Llama ($+0.31^{**}$ with the lexical block; $+0.21^{*}$ surviving PR) but null in Gemma ($-0.01$) --- reported as a non-replicating candidate, not among the laws. (1 para + table)
\item \textbf{R3: Spectral metrics: every spectral metric carries signal, and each alone beats the whole lexical block.} Llama, one-at-a-time: $\lambda_1$/trace $+0.55^{**}$ (AIC 778.5), $\lambda_1/\lambda_2$ $+0.57^{**}$ (775.3), PR $-0.49^{**}$ (786.5) vs.\ lexical block 799.0, null 816.4; best model lex$+$PR (763.6, PR $-0.62^{**}$). Concentrated discriminative spectra steer; polysemy is largely absorbed by PR in the joint model --- polysemous words appear to fail \emph{via} diffuse spectra. \textbf{Instrument caution (one of our sharpest methodological findings): under the dual v2b frame $\lambda_1$/trace was \emph{null} --- the register axis parked in within-positive variance can capture the top eigenvector for weak concepts (Appendix~B), corrupting spectral readouts. Spectral diagnostics are only interpretable relative to the contrast design.} Spectral metrics are near-collinear ($r{\le}-.94$): one-at-a-time only. (1 para)
\item \textbf{Category is partly mediated by measurable word properties.} Supersense dummies beat the null (AIC 784.1 vs.\ 816.4) but lose to lexical$+$PR (763.6), and adding them to the best model does not pay: the category signal visible in R1 is substantially carried by the continuous predictors. (Under v2b the dummies did not even beat the null; the wider v4 spread carries real categorical signal.) (half para)
\item \textbf{R4: The rescue probe (causal).} Design: for failing concepts, cancel the frame/parent component --- either by re-extracting with within-category contrasts (``loves Edinburgh'' vs.\ ``loves \{other city\}''; not yet run) or by subtracting the leave-one-out supersense mean from the saved direction ($v_{\mathrm{spec}}$; executed, $n{=}150$). \textbf{Outcome: no net rescue (25 vs.\ 30 of 150 steer; McNemar $p{\approx}.4$) --- shared-component dominance is not the prevailing failure mode, though the subtraction doubles as a frame-leak control. Details moved to Appendix~Z3 [bold note: full $v_{\mathrm{spec}}$ material relocated there; inclusion in the final submission undecided].} (1 para)
\item \textbf{R5: Distributedness.} $\Delta$(all-layer $-$ single-layer): single-layer depth-13 (magn dose) 90/600 (15.0\%) vs.\ all-layer 249 (41.5\%), ratio 2.8$\times$ (v2b: 3.5$\times$); 11 concepts steer \emph{only} single-layer (over-steer rescues; abstract/attribute skew). Caveat from the depth sweep: depth 13 sits below the entity window, so $\Delta$ partly measures window placement --- interpret jointly with the sweep (Discussion). (half para)
\item \textbf{R6: Generality (Gemma-2-9B replication): rates and laws replicate; item convergence is itself instrument-dependent.} Rates: 224/600 (37.3\%) vs.\ Llama 249 (41.5\%); the v4 gain replicates ($\sim$2$\times$ both models' v2b rates). Laws: all four lexical signs replicate (AoA $-0.49^{**}$ strongest); PR replicates \emph{at Gemma's informative depth} (60--70\%: $-0.42^{**}$; null at Llama's 40\% convention) --- \emph{same law, model-specific depth}. Items: per-concept $\phi = 0.43$ (154 shared) and supersense-profile $r = 0.44$, both up from v2b (0.33 / 0.12 n.s.): part of the earlier item-divergence was instrument noise, though which concepts steer remains substantially model-relative (Gemma tops: event/phenomenon/group; body bottom in both). Supporting pilot evidence: the $\lambda_1$/trace class ordering \emph{inverts} across models (Llama places $>$ moods; Gemma the reverse) --- spectral values are model-geometry-laden; compare within-model only. Gemma dose grid [14,17,20,23] set by dose-response audit (Appendix~C). (1 para)
\end{outline}

% v2b-instrument regression table moved to Appendix B (tab:reg600).

% main regression table: v4 instrument, both models
\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrr}
\hline
 & \multicolumn{2}{c}{Llama-3.1-8B} & \multicolumn{2}{c}{Gemma-2-9B} \\
 & lexical & $+$PR & lexical & $+$PR$^{60\%}$ \\
\hline
concreteness   & $-0.20^{*}$  & $-0.43^{**}$ & $-0.20^{*}$  & $-0.18^{\dagger}$ \\
Zipf frequency & $+0.28^{*}$  & $+0.23$      & $+0.23^{\dagger}$ & $+0.20$ \\
AoA            & $-0.33^{**}$ & $-0.28^{*}$  & $-0.49^{**}$ & $-0.47^{**}$ \\
polysemy       & $-0.30^{**}$ & $-0.15$      & $-0.35^{**}$ & $-0.29^{*}$ \\
n tokens       & $+0.04$      & $-0.05$      & $+0.06$      & $+0.02$ \\
participation ratio &         & $-0.62^{**}$ &              & $-0.39^{**}$ \\
\hline
AIC            & 799.0        & 763.6        & 767.3        & 750.9 \\
\hline
\end{tabular}
\caption{\textbf{Main regression (v4 instrument).} Logistic regression of
steerability (V1 $\cup$ V6) on z-scored predictors, $n{=}600$ per model, all-layer
config. PR read at each model's informative depth (Llama 40\%, Gemma 60\% ---
Gemma's spectral signal is null at the 40\% convention and switches on at 60--70\%).
Reference AICs --- Llama: null 816.4, supersense-only 784.1, one-spectral-alone
775--787; Gemma: null 794.8. Spectral collinearity is extreme under v4
(PR--$\lambda_1$/trace $r \le -.94$ both models): spectral metrics are reported
one-at-a-time only. $^{\dagger}p{<}.1$, $^{*}p{<}.05$, $^{**}p{<}.01$.}
\label{tab:reg600v4}
\end{table}

\section{Discussion (1.0p)}
\begin{outline}
\item What makes a concept steerable: attributes (context-general colorings) admit a single injectable direction; instance identities require assembled, multi-stage content --- steering failure is usually \emph{delivery of the parent category}, not silence. (1 para)
\item \textbf{What the spectral metrics are: an intervention-match score, with an instrument caveat.} Spectrum \emph{concentration} (low PR / high $\lambda_1$-share) measures how well the concept's discriminative signature fits the one move single-vector steering can make (one constant vector, all contexts); it predicts steering in both models. \textbf{The mechanism behind R2's reversal: \emph{abstract} (not concrete) concepts steer better} --- for concrete instance-like concepts the dominant direction is largely the shared category axis, so its coherence does not deliver the instance (parent-dominance). \textbf{And the caveat: spectral diagnostics of extracted directions are only interpretable relative to the contrast design that produced them} --- under the dual v2b frame $\lambda_1$/trace read as null because a frame-register axis could capture the top eigenvalue slot (Appendix~B); under the constant v4 prefix it predicts steering with the expected sign. (1 para)
\item \textbf{Measured steerability is instrument-dependent --- and so is cross-model generality.} Changing one frame sentence doubles the steerable share in both models (19.8$\to$41.5\% Llama; 18.8$\to$37.3\% Gemma) and \emph{raises} cross-model item agreement ($\phi$ 0.33$\to$0.43) and category-profile agreement ($r$ 0.12$\to$0.44). Absolute rates from any single protocol should not be read as properties of the model alone; but the \emph{laws} (frequency $+$, concreteness $-$, AoA $-$, polysemy $-$, spectrum concentration $-$) survive instrument change and model change, which is the paper's strongest generality claim. (half para)
\item \textbf{Depth structure: concepts live in depth windows, ordered by abstractness.} Attributes are injectable from $\sim$30\% depth onward with a broad plateau; entity identities switch on sharply at $\sim$45\% (window 14--17 of 32) and fade late; personas need multi-site injection. One well-chosen layer nearly matches all-layer steering (71 vs.\ 77 of 90) --- ``all layers'' mostly buys window coverage. Read-side extraction statistics do not locate the window (they miss the late decline entirely): injectability is a write property. (half para)
\item Status of extracted vectors: the residual stream has no privileged basis, so extracted directions are \emph{experiment-relative}; within-model relational claims are valid, coordinate-level and raw cross-model comparisons are not. Anchored spaces (embedding/unembedding, MLP neurons) are where mechanism pins a frame. (half para)
\item Two routes to a vector's identity: \emph{experiment design} (contrasts that vary exactly one thing) vs.\ \emph{causal mediation} (function vectors, ROME). Pipeline: propose by contrast, accept by causal test --- R4 instantiates it. (half para)
\item Practical implications for RePE: report spectral concentration and contrast design alongside steering benchmarks; stratified concept inventories; granularity-graded judging over binary. (half para)
\end{outline}

\subsection*{The depth axis: first-steerable-depth sweep (0.5p)}
% Framing: besides HOW MANY directions compose a concept (the spectral axis),
% WHERE in depth it becomes injectable is a second, near-orthogonal axis.
% Executed on the pilot; WordNet extension is future work.



\begin{outline}
\item \textbf{Design.} Pilot 90 (moods/places/personalities) $\times$ every single-layer depth 10--19, magn dose, v4 directions, V1$\cup$V6 (Figure~\ref{fig:depthsweep}). (half para)
\item \textbf{Result 1: entity concepts have a sharp onset at depth 14 (of 32).} Places: 0--2/30 at depths 10--13, then \textbf{22/30 at depth 14}, window 14--17, fading by 19. This dissolves the earlier ``places don't steer single-layer'' conclusion: the old two-site protocol (13/19) \emph{straddled the window}. (half para)
\item \textbf{Result 2: attribute concepts steer shallow and everywhere.} Moods: 16/30 already at depth 10, plateau 25--30 from depth 13 through 19 --- attributes become injectable $\sim$4 layers before instances (the abstraction-depth hypothesis, confirmed ordinally). Personalities never find a single-layer home (peak 20, collapse to 1 by depth 19) --- persona content appears genuinely multi-site. (half para)
\item \textbf{Result 3: one well-chosen layer $\approx$ all layers} (71/90 at depth 15 vs.\ 77/90 all-layer): the all-layer advantage is largely \emph{not knowing the window}, not distributed injection per se (except personalities). Per-layer read-side metrics rise into the window but entirely miss the late decline ($|r| \approx .5$--$.6$): first-steerable-depth is a \emph{write} property. (half para)
\item \textbf{Future work:} same sweep on the six featured WordNet cells (150 concepts) with taxonomic depth as ground truth (hypernyms shallower than hyponyms?); independence from the spectral axis assessed within supersense; compositional vignette --- is direction(``strict father'') $\approx \alpha\,$direction(``strict'') $+ \beta\,$direction(``father'')? (half para)
\end{outline}

\section{Limitations (0.5p; uncounted in ACL format)}
\begin{outline}
\item Single extraction method (RFM); $\lambda_1$/trace is AGOP-specific --- PCA explained-variance as cross-check (appendix if time). (half para)
\item Steering conclusions are relative to the additive intervention class; clamp/projection interventions may behave differently (targets saved, untested). (half para)
\item LLM judge (validated but imperfect); English only; dominant-sense assumption; norms are human ratings. (half para)
\item Correlational core with one causal probe (R4); extraction-design effects and model limits not fully separable outside that design. (half para)
\item Depth-sweep evidence is pilot-only ($n{=}90$, 3 concept classes, one model); the WordNet-wide sweep and the Gemma sweep are future work. Measured rates are instrument-relative (quantified, but a reader should not quote ``41.5\%'' as a model constant). (half para)
\item The v4 frame's constant prefix puts an imagine/assume register component into every direction; the frame-leak control ($v_{\mathrm{spec}}$ subtraction, Appendix~Z3) was run on the v2b instrument only and not repeated for v4 --- some v4 successes may be partly frame-carried. (half para)
\end{outline}

% \section*{Ethics Statement}  % likely minimal
% \section*{Acknowledgments}

% acl.sty already sets \bibliographystyle{acl_natbib}; do not repeat it.
\bibliography{refs}
% refs.bib created jul10 (~28 entries; 2 stubs marked TODO inside: Billa/LAP and
% "When is your LLM steerable?"). Inline author-year mentions in Background can now
% be converted to \citep/\citet as sections are prosed.

\input{appendix_experiments}

\end{document}
